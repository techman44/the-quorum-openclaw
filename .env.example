# The Quorum for OpenClaw - Docker Environment Configuration
# Copy to .env and adjust values as needed.
# These variables are used by docker-compose.yml and the install script.

# ── PostgreSQL ──────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USER=quorum
DB_PASSWORD=GENERATE_ON_INSTALL
DB_NAME=quorum

# ── Ollama ──────────────────────────────────────────────────
OLLAMA_HOST=http://localhost:11434
OLLAMA_PORT=11434
OLLAMA_EMBED_MODEL=mxbai-embed-large
EMBEDDING_DIM=1024
